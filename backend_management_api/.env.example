# Server Configuration
HOST=0.0.0.0
PORT=3001
NODE_ENV=development

# CORS Configuration
BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000
WS_URL=ws://localhost:3001/ws
SITE_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4000
ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_MAX_AGE=3600
COOKIE_DOMAIN=localhost
TRUST_PROXY=true

# Request Configuration
REQUEST_TIMEOUT_MS=30000
RATE_LIMIT_WINDOW_S=60
RATE_LIMIT_MAX=100

# Database Configuration
# NOTE: These values are set by the deployment system based on the database container
# POSTGRES_HOST will be set automatically
# POSTGRES_PORT will be set automatically
# POSTGRES_DB will be set automatically
# POSTGRES_USER will be set automatically
# POSTGRES_PASSWORD will be set automatically

# JWT Configuration
# IMPORTANT: Generate secure random secrets for production
# Example: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=your-secure-access-secret-change-in-production
JWT_REFRESH_SECRET=your-secure-refresh-secret-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Password Hashing Configuration
# Recommended: 12 or higher for production (higher = more secure but slower)
BCRYPT_SALT_ROUNDS=12
